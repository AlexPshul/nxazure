{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Publish executor",
  "description": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The Azure Function App name to publish to. Environment variable templates such as {FUNCTION_APP_ENV} are expanded before publishing.",
      "alias": "n",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "additionalFlags": {
      "type": "string",
      "description": "Additional flags to pass to the func start command. (port, cors, etc.)",
      "x-prompt": "Are there any additional flags to start the Azure Functions runtime?"
    }
  }
}